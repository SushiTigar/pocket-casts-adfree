# Runtime dependencies for the Pocket Casts Ad-Free Pipeline.
#
# Pinned to the exact versions used in development. Bump deliberately
# (one library at a time + run tests.py) rather than letting pip resolve
# transitive drift.

httpx==0.28.1
mutagen==1.47.0
Flask==3.1.3
Pillow==12.2.0

# Optional: enables the rendered /readme view in the dashboard.
# Falls back to a plain <pre> dump if missing.
Markdown==3.7

# Locked transitive dependencies (kept for reproducible installs).
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.2
blinker==1.9.0
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
sniffio==1.3.1
idna==3.11
certifi==2026.2.25
